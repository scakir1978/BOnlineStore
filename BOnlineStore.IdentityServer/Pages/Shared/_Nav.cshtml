@using Duende.IdentityServer.Extensions
@{
    string name = null;
    if (!true.Equals(ViewData["signed-out"]))
    {
        name = Context.User?.GetDisplayName();
    }
}

<div class="layout-width">

    <nav class="navbar-header">

        @*<h2 class="d-flex" style="text-align:center; width:100%">BOnline Store</h2>*@

        @*<a href="~/" class="navbar-brand">
            <img src="~/duende-logo.svg" class="icon-banner">
            BOnline Store
        </a>*@

        @if (!string.IsNullOrWhiteSpace(name))
        {
            <div class="btn-group">
                <button type="button" class="btn btn-primary dropdown-toggle"
                        data-bs-toggle="dropdown" aria-haspopup="true"
                        aria-expanded="false">
                    @name
                </button>
                <div class="dropdown-menu">
                    <a class="dropdown-item" asp-page="/Account/Logout/Index">Oturumu Kapat</a>
                </div>
            </div>            

            @*<ul class="navbar-nav mr-auto">
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">@name <b class="caret"></b></a>
                    
                    <div class="dropdown-menu">
                        <a class="dropdown-item" asp-page="/Account/Logout/Index">Logout</a>
                    </div>
              </li>
            </ul>*@
        }
    
    </nav>
</div>
